# [[.AppName]]

基于 [Go](https://go.dev/) 与 [Kratos](https://go-kratos.dev/) 框架的微服务，目录结构遵循 `kbfs_prediction` 约定。

## 技术栈

- 语言：Go (>= 1.23)
- 框架：Kratos v2
- 依赖注入：google/wire
- 通信协议：HTTP / gRPC

## 目录结构

```bash
.
├── cmd/services/api/   # 服务入口（main + wire）
├── biz/                # 业务代码
│   ├── handler/        # 接入层：HTTP / gRPC handler
│   ├── app/            # 应用层：用例编排
│   ├── domain/         # 领域层：核心业务逻辑
│   └── consts/         # 常量
├── infra/              # 基础设施
│   └── conf/           # 配置加载与定义
├── pkg/utils/          # 通用工具
├── server/             # HTTP / gRPC server 装配
├── configs/            # 分环境配置（config_<env>.yaml）
├── docker/             # 本地依赖容器编排
├── Dockerfile
├── Makefile
└── go.mod
```

## 快速开始

```bash
# 安装工具并整理依赖
make init
go mod tidy

# 生成 wire 注入代码
make generate

# 本地运行（默认读取 configs/config_dev.yaml，可通过 APP_ENV 切换环境）
make run-api
```

## 配置

配置文件按环境拆分：`configs/config_<APP_ENV>.yaml`，默认 `dev`。

```bash
APP_ENV=prod ./bin/api
```

> 提示：本骨架默认使用文件配置与公共依赖。如需接入组织内部的 Nacos 配置中心、
> MySQL/Redis/RabbitMQ 等私有 SDK，请参考 `kbfs_prediction` 的 `infra/` 实现替换对应模块。
